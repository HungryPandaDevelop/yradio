mixin roofContent(topic, offTopic)
  .main-full
    .breadcrumbs
      a(href="#") Главная
      span /
      span= topic
  .content
    if offTopic !=false
      .main-full
        h1= topic
    if block
      block